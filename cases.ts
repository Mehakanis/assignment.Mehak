let userName = 'Noman'  // camelCase
let user_name = 'Mehak' // snake_case
let UserName = 'Ayesha' // PascalCase

// (=)is assignment operator

let isUserExist = true;  
let is_username_exist = false;
let IsUserName = 1;

// data na hoto by default 'any type' ayegi

console.log(userName, user_name, UserName);

console.log(isUserExist, is_username_exist, IsUserName);

let anyType;
// ager tyep bhi na batayen or data bhi na dalain to uska
// ans any ka undefined hoga

console.log(anyType)
